CREATE TABLE  tempdiary (
 	num int NOT NULL AUTO_INCREMENT,
	title varchar(128),
	artist varchar(128),
	email varchar(128),
	username varchar(128),
	comment text,
	registdate varchar(128),
	PRIMARY KEY (num)
);
